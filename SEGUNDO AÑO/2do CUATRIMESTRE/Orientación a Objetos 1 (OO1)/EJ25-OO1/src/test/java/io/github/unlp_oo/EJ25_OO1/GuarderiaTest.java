package io.github.unlp_oo.EJ25_OO1;

public class GuarderiaTest {
	/* Este TEST va a ser teorico (practica para el parcial):
	 
	 En este caso al igual que con los otros servicios, testearia el correcto calculo del mismo.
	 Tendria un objeto de Guarderia para una Mascota (a la cual le agregaria 5 consultas) y otro 
	 objeto guarderia para una Mascota la cual no tiene ninguna consulta anterior.
	  
	  Haria en ambos casos un assertEquals con el costo de cada servicio y esperando que el valor
	  sea el indicado.
	  Seria una prueba con particiones equivalentes (ya que el valor es unico)
	 
	 */
}
